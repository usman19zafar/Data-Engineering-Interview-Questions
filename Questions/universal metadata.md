Every Silver table must include three universal metadata fields:

ingest_file_name

ingest_timestamp

ingest_source_path


A Mapping Data Flow is self‑contained.
It only needs:

a source (your dynamic file)

transformations

a sink (your Silver folder)
